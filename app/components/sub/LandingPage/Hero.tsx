'use client';

import GlobalButton from '../../shareable/Button';

const Hero = () => {
  return (
    <section id="profile">
      <div>
        <div>
          <GlobalButton type="primary" className="text-black">
            Get Started
          </GlobalButton>
        </div>
      </div>
    </section>
  );
};

export default Hero;
