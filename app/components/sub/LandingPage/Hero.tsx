import { Button } from 'antd';
import React from 'react';

const Hero = () => {
  return (
    <section id="profile">
      <div>
        <div>
          <Button type="primary" className="bg-white">
            Book Now
          </Button>
        </div>
      </div>
    </section>
  );
};

export default Hero;
