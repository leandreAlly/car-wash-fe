import { Macondo_Swash_Caps, Overlock } from 'next/font/google';

export const makondo = Macondo_Swash_Caps({
  weight: '400',
  subsets: ['latin'],
});

export const overlock = Overlock({
  weight: '400',
  subsets: ['latin'],
});
